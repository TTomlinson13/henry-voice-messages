## Daily Notes
- Built and launched `second-brain` Next.js app at `/home/todd/.openclaw/workspace/second-brain` with:
  - unified memory/docs/tasks explorer,
  - search and content preview,
  - task API + Kanban phase-2 board,
  - quick task capture,
  - review counters (open/due today/due week/overdue).
- Added Basic Auth protection for second-brain via middleware/proxy gate and `.env.local` credentials.
- Resolved access issues:
  - `.env.local` formatting error (single-line env) fixed,
  - password parsing issue with `$` avoided by using simple delimiter-safe password,
  - confirmed access works through SSH local tunnel (`127.0.0.1:3000`).
- Added recurring second-brain cron jobs:
  - daily summary (8:00 AM ET),
  - weekly review (Mon 8:15 AM ET),
  - overdue alerts (10:00/14:00/18:00 ET, notify only if overdue > 0).
- Expanded morning brief cron payload to include:
  1) relevant news,
  2) business ideas,
  3) today’s tasks,
  4) recommendations for Henry+Todd collaborative tasks.
- Produced and emailed `CANOPY-QUOTERUSH-OPTIMIZATION-RUNBOOK.pdf`.
- Captured canonical Canopy Connect agency link for default use in ad copy/workflows:
  `https://app.usecanopy.com/c/tomlinson-and-co`
- Built FL homeowner growth assets:
  - 30-ad homeowner swipe file with embedded Canopy link.
  - `ops/runbooks/fl-homeowner-lead-plan.md` and emailed PDF `FL-HOMEOWNER-LEAD-PLAN.pdf`.
  - Generated 50 prebuilt UTM campaign links at `ops/FL-UTM-LINKS-50.csv`.
  - Generated Bitly-ready import file `ops/FL-BITLY-IMPORT-50.csv` and emailed both CSVs.
- Lead automation strategy advanced significantly:
  - Created `ops/runbooks/homeowner-lead-agent-blueprint.md` and emailed PDF `HOMEOWNER-LEAD-AGENT-BLUEPRINT.pdf`.
  - Created `ops/homeowner-lead-agent-pilot-checklist.md`.
  - Added pilot rhythm cron jobs:
    - `homeowner-lead-pilot-daily-standup` (9:00 AM ET)
    - `homeowner-lead-pilot-eod-review` (5:30 PM ET)
- Voice strategy for lead-call agents documented:
  - Defined voice pack roles (Avery intake, Mason follow-up, Elena close-out/escalation).
  - Created `ops/runbooks/homeowner-call-scripts.md` and emailed PDF `HOMEOWNER-CALL-SCRIPTS.pdf`.
- Partner/data acquisition context confirmed:
  - Todd has tools/databases for loan officers, realtors, closing agents and expired leads across FL + 3 states.
  - Compliance-first approach emphasized for outreach and automation.
- Morning brief preferences expanded and confirmed useful:
  - relevant news, business ideas, today tasks, and Henry+Todd collaborative recommendations.
- Business development context captured:
  - Todd has a searchable database/program for local/state/regional loan officers, realtors, and closing agents.
  - Todd also has expired-lead databases for FL + 3 other states (name/address/phone/email).
  - Intent is to build partner-led + reactivation lead engines with compliance guardrails.
- Platform stack clarification from Todd: they actively work with Manus, Loveable, and Lindy.
- Began live guided build of Lindy-based Carrier QRG auto-update automation:
  - Flow architecture moved to schedule -> loop(rows) -> agent step (JSON output) -> condition branches.
  - Branch logic drafted: no_change, auto_publish (confidence >= 0.85), pending_approval.
  - Major blockers encountered in Lindy: publish/save instability, stale/buggy Google field loaders, and Google connection not binding (`Used by 0`).
  - Temporary operational workaround chosen: use `QRG-INBOX-TEMP` upload staging folder until direct destination-folder binding is stable.
  - Additional shared folder received for destination context: `T & C- Carriers Contracts /agreements`.
- Security/access context expanded:
  - Todd shared access to `TC Master Passwords` Google Sheet for setup support.
- Website portfolio update from Todd:
  - `www.tcpolicy.com` (built with Manus, previously discussed)
  - `www.hoinsurance.com` (boutique insurance site)
  - `www.floridauto.com` (boutique insurance site)
  - `www.branchagent.com` (used for carrier access with staff + branch agents; review requested)
- Branchagent access update:
  - Login details were sent by Todd via email.
  - Next step is a full portal walkthrough/review once browser relay session is attached.
