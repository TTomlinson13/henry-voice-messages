# 2026-02-19

## 12:11 PM - American Integrity Policy Transaction Report
Todd forwarded a policy transaction report from American Integrity Insurance Group (contact@aii.com).
*   **Context:** Example of a report they receive for policy activity.
*   **Content:** Cancellation Notice for Non-Payment.
*   **Policy:** AGH313180 (Inception: 2014-11-24).
*   **Insured:** Joseph Jeffery and Rosemary Jeffery.
*   **Amount:** -$2,687.54.
*   **Date Processed:** 2026-02-17.

## Operations and automation updates
- Confirmed recurring CD relay behavior remains tab/session-scoped; Todd continues using quick re-attach workflow when ON badge drops.
- Successful live reconnects to Client Dynamics through attached Chrome relay tab; on-demand checks can run when tab is attached.
- Clarified tunnel usage for Todd: laptop 18789/18792 tunnel is only needed for local dashboard/control access; always-on machine focuses on relay-tab stability.
- BranchAgent retry attempted from active browser context: site opened, but interaction failed because relay remained attached only to CD tab; next step is attaching relay on BranchAgent tab and retrying.
- Created and sent `ops/runbooks/manus-loveable-routing-spec-v1.md` to Todd (Manus + Loveable specialist-lane orchestration spec).
- Confirmed plan to integrate Manus and Loveable via APIs when Todd provides endpoint/auth docs.
- Added creative media routing to `ops/runbooks/model-routing.md`:
  - Video lane primary: Google Veo
  - Image lane primary: Nano Banana
  - Included input/output contracts and routing triggers for combined campaign creative workflows.
- Multiple live CD reconnections succeeded after relay drops; reaffirmed operating pattern to use existing attached tab context (avoid opening unnecessary new control contexts).
- Confirmed CD API access path via Jamie email: docs at `https://api-docs.clientdynamics.com` and credentials available in CD Agency Settings.
- BranchAgent relay attach reached interactive state: login page rendered and credential submit attempted from attached tab.
- BranchAgent control remains fragile after submit (tab context detaches); immediate recovery pattern is re-attach relay on BranchAgent tab and continue from active tab.

## Pre-compaction flush
- BranchAgent was re-attached and confirmed interactive on dashboard while logged in as `henry@usicna.com`.
- Observed BranchAgent state snapshot: `My Credentials` at `0 / 144 carriers`; Carrier Directory + LOB Finder fully loaded (`144 of 144 carriers`) with filters (state + Auto/Home/Commercial/Specialty).
- Next blocked step remains CD API credential pull from Client Dynamics Agency Settings; waiting for relay attach on the CD tab (`"cd attached"`).

## Evening updates (integrations + data ops)
- Manus API access confirmed inside Manus Integrations UI (`Build with Manus API`).
- Manus endpoint/auth pattern captured from quickstart:
  - Base URL: `https://api.manus.ai/v1`
  - Create task: `POST /tasks`
  - Header auth format: `API_KEY: <MANUS_API_KEY>`
- Todd rotated/deleted previously exposed Manus keys and moved to safer handling.
- Loveable workspace currently does not expose direct account API key controls in settings/labs/connectors; support request submitted to enable API access and provide base URL/auth/endpoints/webhook/rate-limit details.
- Loveable fallback validated: in-app workflow execution path is usable now while waiting for official API enablement.
- 8x8 API request drafted/sent; Phase 1 ask set to:
  - Real Time Statistics Reporting API
  - Contact Center Statistics Reporting API
  - Case and Contact Management API
  (with recordings/transcripts + recording control deferred to Phase 2)
- Messaging/output style preference locked further: short clean paragraph breaks, plain text, easy reading, no markdown artifacts or escaped characters (e.g., literal `\\n`).
- Citizens access context updated: credentials are in shared Google Sheet (`T C Master Passwords`), and Citizens login requires Microsoft email authenticator code sent to Todd.
- Carrier login naming note captured: Universal North America Insurance Company still uses UIHNA identity for system login/portal context (`www.uihna.com`).
- Florida voter DVD workflow advanced:
  - DVD copied from laptop and transferred to always-on OfficeServer.
  - Staged path: `C:\OpenClaw\data\flvoters\DVD_2026-02-19`
  - Extraction completed successfully: 134 files, ~4.57 GB extracted.
  - Delimiter profiling complete: all 134 files detected as tab-delimited.
  - Normalization script execution in progress (PowerShell quoting/continuation issues worked around with script-file approach).

## Late-night operations flush
- CD admin-level Agency Settings/API page became accessible via relay under Todd-admin context (`agency-profile.php`).
- Confirmed CD API values and auth pattern from live page + docs:
  - Agency ID: `7770898979`
  - Tenant base domain: `tomlinson-and-co.clientdynamics.com`
  - API docs: `https://api-docs.clientdynamics.com/`
  - Required headers: `X-Agency-Id` and `X-API-Key`
  - Base API form: `https://<tenant>.clientdynamics.com/api`
- Noted security follow-up: API key appeared in shared screenshot/chat context; rotate key after first integration validation.
- Relay status check: CD tab re-attached and controllable; BranchAgent also re-attached and available.
- Loveable status unchanged: fallback in-app workflow works; official external API details still pending support response.
- Florida voter ingest progressed to long-running chunked normalization in Command Prompt; observed steady county-by-county processing (e.g., ~13/67 at one checkpoint) and instructed to let run continue uninterrupted.
- Mission Control snapshots automation added (`mission-control-snapshot-3x-daily`) at 9:00 AM, 1:00 PM, 6:00 PM ET with NO_REPLY when no meaningful changes.
- OpenClaw version check run on host:
  - Current installed: `2026.2.9`
  - Update available: `2026.2.19-2` (`openclaw update`)
  - User confirmed update awareness; waiting for explicit go-ahead to run update.
