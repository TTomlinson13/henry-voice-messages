
## Durable notes (compaction flush)
- Todd confirmed Henry mailbox identity should be `henry@usicna.com` (without the extra "i").
- Gmail Pub/Sub webhook pipeline is configured on project `genial-union-486906-e6` and tied to OpenClaw hooks (`/hooks/gmail`) with Tailscale funnel path `/gmail-pubsub`.
- Major blocker was gog keyring decrypt mismatch (`aes.KeyUnwrap integrity check failed`); resolved by using the real keyring passphrase in `~/.config/openclaw/gog.env` and restarting gateway.
- OpenClaw config now includes `hooks.mappings` for path `gmail` with `deliver: true` and `channel: "last"` so Gmail events are delivered to chat.
- Gateway is currently healthy after restart (`openclaw gateway status` showed RPC probe ok).

## Durable notes (compaction flush 2)
- Gmail webhook ingestion is now functioning; hook notifications are reaching the session and reported "OC DELIVER TEST" from Todd.
- OpenClaw config was patched to include a Gmail hook mapping with `deliver: true` and `channel: "last"` for chat delivery.
- Remaining issue: direct `gog gmail send` from this shell still fails intermittently with `aes.KeyUnwrap(): integrity check failed` (keyring/passphrase mismatch in some contexts).
- Todd asked the assistant to send an outbound test email to `tt@usicna.com`; send attempt failed due to the keyring decrypt error.

## Durable notes (compaction flush 3)
- **Gmail Integration:** Fully operational. Watcher is healthy (`history_id` 2956+). Inbound emails trigger hooks and are delivered to the Telegram chat. Outbound sending tested successfully.
- **Troubleshooting Resolution:** The `aes.KeyUnwrap` error was resolved by re-authenticating `gog` manually and ensuring the `GOG_KEYRING_PASSWORD` in `~/.config/openclaw/gog.env` exactly matched the new passphrase.
- **Cron Job:** Created `daily-weather-32714` to report weather for zip 32714 every day at 6:30 AM ET.
- **User Context:** Confirmed Todd's location as Altamonte Springs, FL (32714).
